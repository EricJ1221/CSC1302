// Augmented Operators

public class AugmentedOperators {
   public static void main(String[] args) {
      int count = 0;
      /*
      if(studentGroupEntered)
      */
      count += 5; // count = count + 5; // count = 5;
      count *= 2; // count = count * 2; // count = 10;
      count -= 1; // count = count - 1;
      System.out.println(count);
   }
}